#include<bits/stdc++.h>
using namespace std;
using ll=long long;
void solve()
{
    ll n,m,i,j;
    cin >> n >> m >> i >> j;
    cout << 1 << " " << 1 << " ";
    cout << n << " " << m;
    cout << "\n";
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while(t>0)
    {
        t--;
        solve();
    }
    return 0;
}